#include <iostream>
#include <SDL.h>
#include <SDL_image.h>
#include <SDL_ttf.h>

void initializeGame(bool appRunning) {
    appRunning = true;
}

int main()
{
    bool appRunning = false;

    initializeGame(appRunning);

    do {

    } while (appRunning);

    return 0;
}

